import 'package:flutter/material.dart';
class jobsapplied extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:Center(child: Text('Jobs Applied')),
    );
  }
}